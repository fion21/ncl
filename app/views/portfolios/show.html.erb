<% if @portfolio_item.main_image.present? %>
  <%= image_tag @portfolio_item.main_image.url %>
<% end %>

<h1><%= @portfolio_item.title %></h1>
<em><%= @portfolio_item.subtitle %></em>

<p><%= @portfolio_item.body %></p>

<% if @portfolio_item.thumb_image.present? %>
  <%= image_tag @portfolio_item.thumb_image.url %>
<% end %>
